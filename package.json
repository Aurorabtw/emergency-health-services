{
  "name": "health-hub-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-health-hub-rules \"npm run test:rules\"",
    "test:rules": "node --test --test-concurrency=1 rules-tests/firestore.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}
